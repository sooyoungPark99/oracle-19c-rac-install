# rac2 노드 생성

### 1. rac1 VM 종료(복제 전)

- 복제 전에 반드시 rac1을 먼저 종료한다.

```bash
[root@rac1 ~]# shutdown -h 0 -f
```

---

### 2. CMD를 관리자 권한으로 열고 VDI 복제 (호스트: Windows)

```bash
# cmd를 관리자 권한으로 열고 rac1 폴더로 이동
D:
cd D:\oraclevm\rac\rac1

# VDI 복제 (폴더를 만들지 않고 상위 폴더에 복제)
VBoxManage clonemedium disk rac1.vdi ..\rac2.vdi
```

#### 폴더 구조 확인

완료 후 `D:\oraclevm\rac\` 구조 예시

```
rac\
├── rac1\      ← rac1.vdi
├── rac2\      ← rac2.vdi  (새로 생성됨)
└── shared\    ← 공유 디스크 (CRS, DATA, FRA)
```

---

### 3. VM 생성 및 기본 설정

1. **아래 이미지대로 생성**

<img width="804" height="404" alt="image" src="https://github.com/user-attachments/assets/10348540-b607-4072-a695-7755462cf280" />

<img width="803" height="377" alt="image" src="https://github.com/user-attachments/assets/b182c0a1-091f-4f2c-88c7-b77e848e4290" />

<img width="802" height="373" alt="image" src="https://github.com/user-attachments/assets/9da9f684-910e-4129-abe0-0e3f9acb9395" />

#### **2. rac2.vdi를 rac2 폴더 안으로 이동 (관리자권한 CMD에서)**

```sql
move D:\oraclevm\rac\psy2.vdi D:\oraclevm\rac\rac2\rac2.vdi
```

#### **VM 새로 만들기로 rac2 VM 을 생성하고 아래와 같이 설정한다.**

| 항목 | 설정 |
| --- | --- |
| 시스템 | 플로피 디스크 해제 |
| 오디오 | 해제 |
| 저장소 | `rac2/rac2.vdi` 연결 + 공유 디스크 CRS, DATA, FRA 할당 |
| 네트워크 어댑터 1 | NAT (단순) |
| 네트워크 어댑터 2 | 호스트 전용 어댑터 |

#### 설정 화면 캡처(네트워크/시스템/저장소/오디오)

- 네트워크 설정

<img width="739" height="482" alt="image" src="https://github.com/user-attachments/assets/6ff22229-ffc4-4361-9f52-7e30b29b0d46" />

<img width="738" height="484" alt="image" src="https://github.com/user-attachments/assets/79f4c547-ed2f-4620-819d-1fb8286be852" />

- 시스템 설정

<img width="739" height="489" alt="image" src="https://github.com/user-attachments/assets/5aeb6e0d-f030-44c9-b714-73641ae71c6b" />

- 저장소 설정

<img width="1172" height="634" alt="image" src="https://github.com/user-attachments/assets/b7e83261-734a-49fc-b105-56976b6388f6" />

- 오디오 해제

<img width="737" height="502" alt="image" src="https://github.com/user-attachments/assets/090725b3-b164-430a-8e39-6d0bf9cfa3c7" />

---

### 4. rac2 네트워크 IP 설정(리눅스)

- VM 실행 후 네트워크 설정에 들어간다.

**VirtualBox NAT 네트워크 게이트웨이는 `10.0.2.2` 이다.** (`.1` 아님)


| 항목 | rac2 설정값 |
| --- | --- |
| IP 주소 | `10.0.2.16` |
| 서브넷 마스크 | `255.255.255.0` |
| 게이트웨이 | `10.0.2.2` |
| DNS | `168.126.63.1` |
- **enp0s3**: 위 값으로 설정 (인터넷, yum 용)

<img width="979" height="686" alt="image" src="https://github.com/user-attachments/assets/dc99ac54-5994-4b0c-95ec-b77265bca7bb" />

- **enp0s8**: `192.168.56.102` 설정 (rac1 ↔ rac2 인터커넥트 용)

<img width="976" height="686" alt="image" src="https://github.com/user-attachments/assets/1706199a-6bf9-4ac2-99e6-045a3dd6650f" />

---

### 5. 호스트 이름 변경

- MobaXterm으로 접속한다.

<img width="898" height="606" alt="image" src="https://github.com/user-attachments/assets/81d23189-f92c-4f85-8d33-99d9689f3fd8" />

```bash
hostnamectl set-hostname rac2
```

<img width="583" height="48" alt="image" src="https://github.com/user-attachments/assets/df1902a9-d89d-4ba7-b5cc-f9a561c2c266" />

---

### 6. oracle 계정 `.bash_profile` 수정

- 1번 노드와 다른 부분만 변경한다.

```bash
[root@rac1 ~]# su - oracle
[oracle@rac2 ~]$ vi .bash_profile

export ORACLE_SID=rac2          # ← 2번 노드
export ORACLE_HOSTNAME=rac2     # ← 2번 노드
export GRID_SID=+ASM2           # ← 2번 노드 ASM 인스턴스
```

- 나머지(ORACLE_BASE, ORACLE_HOME, ORACLE_UNQNAME, GRID_HOME, alias 등)는 1번 노드와 동일하다.

<img width="1104" height="885" alt="image" src="https://github.com/user-attachments/assets/5fd65fad-b303-4ef5-a019-b52515913209" />

%s/rac1/rac2/g 로 한번에 변경해준다


 `+ASM1`도 `+ASM2`로 변경한다.

<img width="1011" height="880" alt="image" src="https://github.com/user-attachments/assets/24a463f1-3ddb-47d2-b8de-004e580218af" />


저장 후 `.bash_profile`을 적용한다.

```bash
[oracle@rac2 ~]$ source .bash_profile
```

---

### 7. 디렉터리 생성 및 권한 설정(root)

- rac2에서 **root**로 수행한다. (GRID 설치 전 권한 문제 방지)

```bash
mkdir -p /u01/app/oracle/product/19c
mkdir -p /u01/app/grid/19c
mkdir -p /u01/oraInventory
mkdir -p /oraarch
chown -R oracle:dba /u01
chmod -R 775 /u01
chown -R oracle:dba /oraarch
chmod -R 775 /oraarch
chown -R oracle:dba /dev
```

<img width="659" height="271" alt="image" src="https://github.com/user-attachments/assets/35522166-eaad-420f-a523-3320bf538250" />

---

### 8. 재부팅 및 IP 확인

```bash
reboot
```

- 재부팅 후 IP 주소가 정상 설정되었는지 확인한다.

<img width="1234" height="829" alt="image" src="https://github.com/user-attachments/assets/9ef98d3e-95dc-4dfc-abea-0cd8f4817f81" />

---

#### 핵심 포인트

- `vboxmanage clonemedium`은 폴더를 만들지 않으므로 **`mkdir rac2`를 먼저** 실행해야 한다.
- 게이트웨이는 반드시 **`10.0.2.2`** 이다. (VirtualBox NAT는 `.1`이 아니라 `.2`)
- rac2 변경 필수 4가지: **IP(`10.0.2.16`), 호스트명(`rac2`), ORACLE_SID(`rac2`), GRID_SID(`+ASM2`)**
- 공유 디스크(CRS, DATA, FRA)는 두 노드가 **동일한 디스크를 attach** 해야 한다.
